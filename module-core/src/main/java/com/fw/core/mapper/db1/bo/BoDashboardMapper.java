package com.fw.core.mapper.db1.bo;

import com.fw.core.dto.bo.BoMainDTO;
import org.apache.ibatis.annotations.Mapper;

import java.util.List;

@Mapper
public interface BoDashboardMapper {
    /*BoMainDTO selectTodayTaskInfo(BoMainDTO boMainDTO);
    List<BoMainDTO> selectInflowStats();
    List<BoMainDTO> selectDailySales();
    List<BoMainDTO> selectDailyVisitStats();   // 시간별 (오늘 기준)
    List<BoMainDTO> selectWeeklyVisitStats();  // 날짜별 (이번 주)
    List<BoMainDTO> selectMonthlyVisitStats(); // 날짜별 (이번 달)
    List<BoMainDTO> selectProductSales();*/
}
